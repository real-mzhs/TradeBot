<UserControl x:Class="Desktop.Views.SmallViews.WalletListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Desktop.Views"
             mc:Ignorable="d" 
                     d:Background="#0D0D0D"
        d:DesignHeight="780" d:DesignWidth="1200">
    <Grid>

        <TextBlock Text="List" FontSize="30" Foreground="white"/>


        <!--<Grid Grid.Row="1">

        <DataGrid   
                 ItemsSource="{Binding TradesHistoryCollection}"
                 RowStyle="{DynamicResource DataGridRowStyleSuperAdmine}" ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyleSuperAdmine}" CellStyle="{DynamicResource DataGridCellStyleSuperAdmine}" x:Name="membersDataGrid" Style="{DynamicResource DataGridStyleSuperAdmine}">

            <DataGrid.Columns>

                <DataGridTextColumn Header="Data" Binding="{Binding }" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding }" IsReadOnly="True" Width="*"/>

            </DataGrid.Columns>
        </DataGrid>


    </Grid>-->


    </Grid>
</UserControl>
